// '@napi-rs/canvas' is an optional peer dependency (used only for opt-in PDF
// OCR). Declare it as an ambient module so the codebase type-checks whether or
// not the package is installed.
declare module "@napi-rs/canvas";
